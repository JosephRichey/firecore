% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IsDSTAmbiguous.R
\name{IsDSTAmbiguous}
\alias{IsDSTAmbiguous}
\title{Check if a Date and Time is Ambiguous Due to DST Fall Back}
\usage{
IsDSTAmbiguous(date, time, tz)
}
\arguments{
\item{date}{A character string or Date object representing the date to check,
in a format parseable by \code{as.Date()} (e.g., "2024-11-03").}

\item{time}{A character string representing the time in "HH:MM" or "HH:MM:SS"
format (e.g., "01:30" or "01:30:00").}

\item{tz}{A character string specifying the time zone to check for DST
transitions (e.g., "America/Denver").}
}
\value{
A logical value: \code{TRUE} if the date and time fall within an ambiguous
hour during DST fall back, \code{FALSE} otherwise.
}
\description{
Determines whether a given date and time falls within an ambiguous hour
during Daylight Saving Time fall back transition. In North America, when
clocks "fall back" (typically at 2:00 AM to 1:00 AM), the hour from
1:00-1:59 AM occurs twice, making it ambiguous.
}
\examples{
# Check if 1:30 AM on November 3, 2024 is ambiguous in Denver
IsDSTAmbiguous("2024-11-03", "01:30", "America/Denver")

# Non-ambiguous time on the same day
IsDSTAmbiguous("2024-11-03", "03:00", "America/Denver")

# Time on a non-DST transition day
IsDSTAmbiguous("2024-11-04", "01:30", "America/Denver")

}

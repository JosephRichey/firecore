% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MiscDataTransformations.R
\name{FixColNames}
\alias{FixColNames}
\title{Format Data Frame Column Names for Display}
\usage{
FixColNames(data, prefix = NULL)
}
\arguments{
\item{data}{A data frame whose column names should be formatted.}

\item{prefix}{Optional character string prefix to remove from all column
names after title casing. Common use: removing table name prefixes like
"firefighter_" from column names.}
}
\value{
The input data frame with modified column names.
}
\description{
Converts column names from snake_case to Title Case and optionally removes
a common prefix. Useful for preparing data frames for display in tables
where human-readable column names are preferred.
}
\details{
The function performs the following transformations in order:
\enumerate{
\item Replaces underscores with spaces
\item Converts to Title Case (each word capitalized)
\item Removes specified prefix if provided
}

This modifies the data frame in place and returns it.
}
\examples{
\dontrun{
# Basic usage
df <- data.frame(
  first_name = "John",
  last_name = "Doe",
  hire_date = "2020-01-15"
)

FixColNames(df)
# Column names: "First Name", "Last Name", "Hire Date"

# With prefix removal
df2 <- data.frame(
  firefighter_id = 1,
  firefighter_name = "John",
  firefighter_rank = "Captain"
)

FixColNames(df2, prefix = "Firefighter ")
# Column names: "Id", "Name", "Rank"

# Use in Shiny table output
output$table <- renderTable({
  data |> FixColNames() |> head(10)
})
}

}

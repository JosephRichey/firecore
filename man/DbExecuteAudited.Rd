% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DbExecuteAudited.R
\name{DbExecuteAudited}
\alias{DbExecuteAudited}
\title{Execute a SQL Statement with Audit Logging}
\usage{
DbExecuteAudited(
  sql,
  currentUser,
  context = NULL,
  expectedMin = 1,
  expectedMax = 1,
  showMessage = FALSE,
  notify = TRUE,
  namespace = "DbExecuteAudited"
)
}
\arguments{
\item{sql}{A character string or DBI SQL object containing the
already-interpolated SQL statement to execute. Callers are responsible for
parameterizing the query via \code{DBI::sqlInterpolate()} before passing it in.}

\item{currentUser}{A \code{reactiveVal}, \code{reactiveValues}, or plain character
string identifying the user performing the action. Passed directly to
\code{AuditLog()}.}

\item{context}{Optional character string describing the operation, used in
log messages and the error modal (e.g., \code{"adding a training record"}).
Should start with a verb in gerund form.}

\item{expectedMin}{Minimum number of rows expected to be affected. Default: 1.
Most writes should affect at least one row; set to 0 only for operations
where zero rows affected is a valid outcome (e.g., conditional deletes).}

\item{expectedMax}{Maximum number of rows expected to be affected. Default: 1.
Increase for batch operations.}

\item{showMessage}{Logical. Whether to display a success modal on success.
Default: \code{FALSE}. Set to \code{TRUE} only for the final write in a sequence;
intermediate writes should stay silent.}

\item{notify}{Logical. Whether to call \code{NotifyError()} when the DB execute
fails with an error (not just a row-count mismatch). Default: \code{TRUE}.
Set to \code{FALSE} for writes inside loops where a single failure should not
trigger a notification email.}

\item{namespace}{Character string used as the logger namespace. Default:
\code{"DbExecuteAudited"}. Pass the calling function's name for easier
log filtering (e.g., \code{namespace = "WriteIncident"}).}
}
\value{
Logical. \code{TRUE} if the write succeeded and rows affected is within
\verb{[expectedMin, expectedMax]}. \code{FALSE} otherwise.
}
\description{
Combines \code{DBI::dbExecute()}, \code{AuditLog()}, and \code{CheckWriteResult()} into a
single call. Handles DB errors internally — logs them, sends an error
notification, and returns \code{FALSE} — so callers can accumulate results without
wrapping every write in a \code{tryCatch}.
}
\details{
For transactional operations, call this function inside a \code{tryCatch} block
that manages \code{dbBegin()} / \code{dbCommit()} / \code{dbRollback()}. A \code{FALSE} return
from inside a transaction should trigger a rollback in the caller; this
function does not roll back automatically.

\strong{Execution order:}
\enumerate{
\item Calls \code{DBI::dbExecute()} inside a \code{tryCatch}. On error: logs, optionally
calls \code{NotifyError()}, and returns \code{FALSE} immediately.
\item Logs the SQL statement at \code{INFO} level.
\item Calls \code{AuditLog()} to write to the \code{audit_log} table. Audit log failures
are non-fatal (handled inside \code{AuditLog()}).
\item Calls \code{CheckWriteResult()} to validate the row count and optionally
display a success or error modal.
}

\strong{Note:} This function does not call \code{DBI::sqlInterpolate()}. Parameterize
your query before passing it in to retain full control over the connection
and parameter types.
}
\section{Transactional usage}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{tryCatch(\{
  DBI::dbBegin(app_data$CON)

  results <- c(
    DbExecuteAudited(sql1, current_user, context = "inserting incident"),
    DbExecuteAudited(sql2, current_user, context = "inserting incident unit")
  )

  if (!all(results)) \{
    DBI::dbRollback(app_data$CON)
    return(invisible(FALSE))
  \}

  DBI::dbCommit(app_data$CON)
\}, error = function(e) \{
  DBI::dbRollback(app_data$CON)
  NotifyError(e)
\})
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# Single row insert — silent success, notify on failure
safe_sql <- DBI::sqlInterpolate(
  app_data$CON,
  "INSERT INTO training (firefighter_id, start_time) VALUES (?, ?)",
  ff_id, start_time
)
write_results <- c(
  write_results,
  DbExecuteAudited(safe_sql, current_user, context = "adding a training record")
)

# Final write in a sequence — show success modal
write_results <- c(
  write_results,
  DbExecuteAudited(
    safe_sql,
    current_user,
    context     = "updating attendance",
    showMessage = TRUE
  )
)

# Batch insert loop — suppress per-row notifications
for (ff_id in firefighter_ids) {
  write_results <- c(
    write_results,
    DbExecuteAudited(
      build_sql(ff_id),
      current_user,
      context     = "adding firefighter response",
      expectedMax = 1,
      notify      = FALSE,
      showMessage = FALSE
    )
  )
}

# Conditional delete where 0 rows is valid
DbExecuteAudited(
  delete_sql,
  current_user,
  context     = "removing old units",
  expectedMin = 0,
  expectedMax = 100
)
}

}
\seealso{
\code{\link[=AuditLog]{AuditLog()}}, \code{\link[=CheckWriteResult]{CheckWriteResult()}}, \code{\link[=NotifyError]{NotifyError()}}
}

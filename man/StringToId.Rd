% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MiscDataTransformations.R
\name{StringToId}
\alias{StringToId}
\title{Convert String Value to ID from Data Frame}
\usage{
StringToId(df, column, value)
}
\arguments{
\item{df}{A data frame containing an 'id' column and the search column.}

\item{column}{Unquoted column name to search within.}

\item{value}{The value to search for in the specified column.}
}
\value{
The ID corresponding to the matched value, or a zero-length vector
if no match is found.
}
\description{
Looks up an ID in a data frame based on a string value in a specified column.
The inverse operation of \code{IdToString()}. Commonly used for converting
user-selected display names back to database IDs.
}
\details{
The function expects the data frame to have a column named 'id'. If multiple
rows match the value, only the first match is returned. If no rows match,
an empty vector is returned.
}
\examples{
\dontrun{
firefighters <- data.frame(
  id = 1:3,
  name = c("John Doe", "Jane Smith", "Bob Johnson"),
  rank = c("Captain", "Lieutenant", "Firefighter")
)

# Get ID by name
StringToId(firefighters, name, "Jane Smith")
# Returns: 2

# Get ID by rank
StringToId(firefighters, rank, "Captain")
# Returns: 1

# Non-existent value
StringToId(firefighters, name, "Unknown Person")
# Returns: integer(0)
}

}
